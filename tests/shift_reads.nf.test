nextflow_pipeline {

    name "Test pipeline with --shift_reads"
    script "../main.nf"
    tag "shift_reads"
    // --shift_reads requires all samples to be paired-end (see workflows/atacseq.nf);
    // the default "test" profile mixes single- and paired-end samples, so use
    // "test_controls" instead, which is all paired-end.
    profile "test_controls"

    test("shift_reads") {

        when {
            params {
                outdir = "$outputDir"
                shift_reads = true
            }
        }

        then {
            // stable_name: All files + folders in ${params.outdir}/ with a stable name
            def stable_name = getAllFilesFromDir(params.outdir, relative: true, includeDir: true, ignore: ['pipeline_info/*.{html,json,txt}'])
            // stable_path: All files in ${params.outdir}/ with stable content
            def stable_path = getAllFilesFromDir(params.outdir, ignoreFile: 'tests/.nftignore')
            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    // Number of successful tasks
                    workflow.trace.succeeded().size(),
                    // pipeline versions.yml file for multiqc from which Nextflow version is removed because we test pipelines on multiple Nextflow versions
                    removeNextflowVersion("$outputDir/pipeline_info/nf_core_atacseq_software_mqc_versions.yml"),
                    // All stable path name, with a relative path
                    stable_name,
                    // All files with stable contents
                    stable_path
                ).match() }
            )
        }
    }

    test("shift_reads with stub") {

        options "-stub"

        when {
            params {
                outdir = "$outputDir"
                shift_reads = true
            }
        }

        then {
            assertAll(
                { assert workflow.success },
                { assert snapshot(
                    workflow.trace.succeeded().size()
                ).match() }
            )
        }
    }
}
